package com.localuz.domain.enumeration;

/** The PlanCode enumeration. Immutable business identity of a billing plan. */
public enum PlanCode {
    FREE,
    BRONZE,
    SILVER,
    GOLD,
    PLATINUM,
    FROTTA,
}
